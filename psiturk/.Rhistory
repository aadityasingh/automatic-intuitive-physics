setwd("~/GitHub/HumanGalileo/psiturk")
d = read.csv('parsed_trials.csv')
head(d)
with(d, table(MatchName, ID))
with(d, table(MatchName, TrialType))
with(d, table(ID, TrialType))
head(d)
with(d, table(ID, TrialType, LengthType))
